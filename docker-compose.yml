version: "3.8"
services:
  app:
    build: .
    image: laravel-app:local
    ports:
      - "8080:8080"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: "base64:iSWm+DbKK7BaspRTOP46wVWnQON0tyN8XBweidQkxoc="
      DB_CONNECTION: mysql
      DB_HOST: plchubdb-do-user-24763532-0.j.db.ondigitalocean.com
      DB_PORT: 25060
      DB_DATABASE: defaultdb
      DB_USERNAME: doadmin
      DB_PASSWORD: AVNS_WO0A9eCTVMFKzAtvy_s
    volumes:
      - ./:/var/www/html
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: homestead
      MYSQL_USER: homestead
      MYSQL_PASSWORD: secret
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata:
